{
  "name": "solar-system-explorer",
  "private": false,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run copy-assets && vite build",
    "preview": "vite preview",
    "copy-assets": "node copy-assets.js",
    "vercel-build": "npm run copy-assets && npm run build"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "postprocessing": "^7.0.0-alpha-1",
    "three": "^0.160.0"
  }
}
